# Production talks to the API through the same-origin /api Pages Function proxy
# (functions/api/[[path]].ts), NOT the cross-site *.workers.dev host — that keeps the
# auth cookie first-party. Consumed by axios (src/api/index.ts) and getFileUrl (src/services/files.ts).
VITE_API_URL="/api"

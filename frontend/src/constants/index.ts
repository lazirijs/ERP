export const supportedBadgeColors = [ 'green', 'blue', 'gray', 'yellow', 'red', 'orange' ] as const; // Check @/style.css tailwind @layer base custom classes
export const currency = '€';

export default {
    currency,
    supportedBadgeColors
}